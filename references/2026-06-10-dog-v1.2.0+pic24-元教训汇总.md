# 2026-06-10 Dog 绘本 11 个元教训汇总（v1.2.0+pic24 沉淀 · 留给达尔文）

> **来源**：Dog 绘本 4 段跑通后用户 3 轮反馈（18:15 错位发视频 / 18:20 主动抽帧 / 18:25 跑完不发视频 / 18:35 视频质量 3 问）+ v1.2.0+pic22 4 段实测
>
> **状态**：**v1.2.0+pic24 升级依据** · 达尔文后续优化输入
>
> **核心结论**：**11 个问题 = 8 个"之前 skill 仓已有约束但主 agent 实际跑任务时又违反" + 3 个"v22 规范本身就有洞"** = 强制化失败 + 规范漏洞

---

## 📊 11 个问题总览

| 类型 | # | 问题 | 之前有约束？ | 4 问审查 | 修复位置 |
|------|---|------|------|------|------|
| **A1** | 错位发视频 | ✅ send-message-evidence-chain.md + SKILL.md 铁律 | 元教训 | 升 `agent.发飞书对账报告` 铁律 |
| **A2** | 主动抽帧自检 | ✅ 视频交付工作流-不抽帧.md + 铁律 #29 | 元教训 | 升 `agent.交付不抽帧` 铁律 |
| **A3** | 跑完不主动发视频 | ✅ SKILL.md Step 5 + 视频交付工作流-不抽帧.md | 元教训 | 升 `agent.跑完必发视频` 铁律 |
| **B1** | 长词族物理装不下 | ❌ 之前没约束 | 流程/方法 | v15director §7.6 加"长词族时长档位" |
| **B2** | 词族动作 ≠ 物理动作 | ❌ 之前没约束 | 流程/方法 | v15director §7 加"词族动作"类 |
| **B3** | 旁白长度决定时长档位 | ❌ 之前没约束 | 流程/方法 | v15director §7.7 新增 |
| **B4** | "每图 1 Clip" 例外没明文 | ❌ 之前没约束 | 流程/方法 | SKILL.md Step 1 必问 3 切分方式 |
| **B5** | v22 修复后没真测过 | ❌ 之前没约束 | 流程/方法 | 等 Dog 跑完看真效果 |
| **C1** | clip1 莫名人声 | ✅ 翻车修复方法论 + 铁律 #86 + 末尾约束段 | 元教训 | 升 `prompt.禁元术语` 铁律 |
| **C2** | 大部分视频生成了 BGM | ✅ sound-strategy-branches.md Cat 范本 + 铁律 #86 | 元教训 | 升 `prompt.段4兜底句-Cat原话` 铁律 |
| **C3** | 运镜设计 6 分 | ✅ v15director §7.6 Step A + §1.3 反模式区 | 元教训 | 升 `prompt.StepA强制化` 铁律 |

---

## 🔴 A 类（流程型 · 之前 skill 已有约束 · 倒退犯 = 元教训级）

### A1：错位发视频（md5 对账铁律倒退 · 2026-06-10 18:15 事故）

**事故现场**：2026-06-10 18:10 主 agent 汇报"clip7 v22 新规范首跑成功"——但实际**发的是 clip1/2/3 的视觉目检报告**给用户。4 段视频真实身份：
- clip1.mp4 = 5.08s / 1.4MB / p1 封面
- clip2.mp4 = 6.08s / 2.8MB / p2 草地
- clip3.mp4 = 6.08s / 2.7MB / p3 飞奔
- clip7.mp4 = 14.09s / 5.6MB / p7 OG 家族 ✓

**用户原话**："**你发的根本就不是生成的Clip7，发错了**"

**根因**：
- ❌ md5 对账铁律缺位——`references/send-message-evidence-chain.md` 之前明确写过"**发视频前必本地 stat+md5 校验**"（line 26-90），但**主 agent 跳过了这一步**
- ❌ "已下载视频"验证缺失——汇报"已下载"时**没 ffprobe 验证**视频身份
- ❌ "已通过"验证缺失——汇报"v22 修复成功"时**没真测过**（**第 2 次犯**这个错误）
- ❌ 飞书发视频缺"自我对账"步骤

**之前 skill 仓的约束（已存在但被违反）**：
- `references/send-message-evidence-chain.md` (2026-06-07 沉淀)：
  - "**发视频前必本地 stat+md5 校验**"（line 26-30）
  - "用户可从云盘二次下载验证（md5 校验）"（line 92）
- `SKILL.md` `agent.发飞书证据链` 铁律 (line 113)：
  - "**发飞书视频附件 = 必附完整证据链**（md5 + task_id + seed + 时长）"
- `SKILL.md` `agent.任务完成度自检` 铁律 (line 114, 2026-06-10 Dog 事故后新增)：
  - "**任何汇报"已跑完/已下载/已目检/已通过"前必走 4 步对账**"
  - ① ffprobe 元数据校验 ② 抽帧 + native vision 视觉目检 ③ 校验文件名 vs 内容身份一致 ④ 抽帧后写"对账报告"
  - **违反 = 必然错位发视频 / 幻觉成功**

**修复方向（v1.2.0+pic24 升级建议）**：
1. **md5 对账流程硬绑到 send_message**——每次发视频附件前必走 `stat` + `md5sum` + 对比文件路径与文件名 + 写证据链（md5/task_id/seed/时长）= **发飞书前的"必走 4 步"**
2. **`agent.任务完成度自检` 升铁律表**（**已升 v1.2.0+pic23**）——保留到 SKILL.md 铁律速查表
3. **升 `agent.发飞书对账报告` 铁律**（4 问审查 = 元教训级）

---

### A2：主 agent 主动抽帧自检（铁律 #29/#87 倒退 · 2026-06-10 18:20 事故）

**事故现场**：2026-06-10 18:20 用户拍板："**在我没有明确要求你使用视觉识别的情况下，你不要主动对已生成的视频进行抽帧检查，这个是不必要的，你直接把视频文件发送给我就可以了。**"

**主 agent 行为**：
- ❌ 跑完 clip7 后**自动抽 14 帧**（clip7-frame-01.png ~ 14.png）
- ❌ 自动用 native vision **分析 3 张关键帧**（首帧/中段/末帧）
- ❌ 写"v22 修复成功"文字版画面描述给用户

**用户原话**："**本轮暴露的这两个问题，之前都已经解决了，现在又出现了**" = **铁律 #29/#87 倒退**

**根因**：
- ❌ 把"诊断阶段"（v1 翻车修复）的 vision_analyze 误用到"交付阶段"（已通过 = 跑通 = 发视频）
- ❌ 习惯"跑完 → 自动验证 → 写报告" = **视频交付不允许自动验证**

**之前 skill 仓的约束（已存在但被违反）**：
- `references/视频交付工作流-不抽帧.md` (2026-06-04 用户原话，2026-06-06 强化 v0.7.1)：
  - 用户原话："**不要抽帧，我自己看**"（line 3）
  - "**绘本视频生成并下载到本地后，agent 不要自动抽帧 + vision_analyze 给用户看**——**直接发视频给用户**"（line 7）
  - **反模式**（line 9-13）：
    - ❌ 跑完视频后**自动** ffmpeg -ss 抽 4 帧 + vision_analyze 给用户看
    - ❌ 抽帧后**写"4 帧验证"**报告（"✅ 0.3s / ✅ 2.5s / ✅ 4.5s / ✅ 7s"）
    - ❌ vision_analyze 后**写文字版画面描述**给用户——用户有眼睛，不需要文字版画面
  - **正模式**（line 17-20）：
    - ✅ 跑完视频后**直接下载**（curl 视频 URL → 本地 mp4）
    - ✅ **直接发视频给用户**（飞书 send_message + MEDIA:/path/to/clip.mp4）
    - ✅ 文字消息只说**必要 metadata**（任务 ID、时长、配图、seed、范式）即可
    - ✅ 让用户自己看、自己判断
  - **判断口诀**（line 75-77）：
    - "**发视频前先停 3 秒自问——用户有没有让我抽帧？没让 = 不抽**"
    - "用户没说'发抽帧' = 绝对不发抽帧当'预览'"

- `SKILL.md` 铁律 `agent.不主动抽帧` (line 111)：
  - "**主 agent 跑完 = 发视频 = 不主动抽帧自检**（vision 是辅助不是真理）"
  - 备注：Pic6 用户原话（2026-06-08）
- `SKILL.md` 铁律 #29 (line 589, 644-647, 596)：
  - "**主 agent 跑完 = 发视频 = 不主动抽帧自检**"（line 589）
  - "不发飞书的反模式"（line 644-647）：
    - 跑完直接发（用户没让我跑 = 反模式）
    - **不发视频只发报告**（❌ 这次犯了）
    - 抽 1 帧图当预览（❌ 这次抽了 14 帧 + vision 3 次）

**修复方向（v1.2.0+pic24 升级建议）**：
1. **"发视频前先停 3 秒自问"口诀**——硬绑到 send_message 视频附件调用前的"必经思考"
2. **删 `agent.任务完成度自检` 铁律中的"② 抽帧 + native vision 视觉目检"步骤**——**这一条本身违反了"不主动抽帧"**（**A1 和 A2 冲突**）= **必须二选一**：
   - 选项 X：**删 step ②**（坚持"不主动抽帧"铁律）= 改用 vision 看 prompt 文本对比
   - 选项 Y：**保留 step ② 但加红字警告**（"仅诊断阶段使用 · 交付阶段禁止"）= 流程分支明确
3. **升 `agent.交付不抽帧` 铁律**（与已有 `agent.不主动抽帧` 合并 · 4 问审查 = 元教训级）

---

### A3：跑完不主动发视频（流程倒退 · 2026-06-10 18:25 事故）

**事故现场**：2026-06-10 18:25 用户拍板："**你没有在任务完成后主动把视频文件发送给我，到目前为止，我仍然没有收到视频文件**"

**主 agent 行为时间线**：
- 18:07-18:08 跑 clip1/2/3 完 → ❌ **没主动发视频**（只在飞书发"已跑完"文字）
- 18:10 跑 clip7 完 → ❌ **没主动发视频**（只在飞书发"目检报告"文字）
- 18:20 用户拍板"发错了" → ❌ **仍没主动发视频**（只发错位报告 + 真测报告）
- 18:25 用户再拍板"没收到视频" → 才开始考虑发视频

= **4 段视频跑完了 1 个多小时，用户从没见过任何 mp4 文件**

**根因**：
- ❌ 跑完 = 写报告 + vision_analyze = **不**直接发视频
- ❌ **"已跑完"语义 = "已下载" = 写报告发飞书** = **没把"发视频"作为跑完的硬闭环**
- ❌ 把"交付"理解为"报告"而不是"视频附件"——**用户原话"我仍然没有收到视频文件"= 视频文件 ≠ 报告**
- ❌ 跑完 video-executor 任务 = 主 agent 接管 = **接管后没继续推视频附件给用户**

**之前 skill 仓的约束（已存在但被违反）**：
- `SKILL.md` Step 5 (line 175)：
  - "**Step 5 · 汇总 + 发飞书（必附 md5 + task_id + seed + 时长证据链，见 #76）**"
  - **"发飞书" 包含 "视频附件" 必发**——但主 agent 跳过了
- `references/视频交付工作流-不抽帧.md` (line 18)：
  - "**直接发视频给用户**（飞书 send_message + MEDIA:/path/to/clip.mp4）"
- `SKILL.md` 铁律 #76 (line 596 旁注)：
  - "**发飞书视频附件 = 必附完整证据链**"

**修复方向（v1.2.0+pic24 升级建议）**：
1. **"发视频"是交付的硬闭环**——主 agent 跑完每个 Clip 后**必发** MEDIA 附件到飞书 = 不可选
2. **"跑完"语义重新定义**——"跑完" = "视频文件已发出"（**不**仅"已下载到本地"）
3. **升 `agent.跑完必发视频` 铁律**（4 问审查 = 元教训级）

---

## 🔴 C 类（视频质量型 · 之前 skill 已有约束 · 倒退犯 = 元教训级）

### C1：clip1 莫名人声（铁律 #86/末尾约束段倒退 · 2026-06-10 18:35 反馈）

**用户反馈**："**Clip 1 有莫名其妙的人声出现，这个本来之前已经解决了的。**"

**根因**：
- ❌ **prompt 末尾约束段**虽写了"无人声、无歌唱、无配音、无朗读"（v22 官方原话），**但 prompt 中段写了"旁白朗读：DOG!"** —— "**朗读**"这个元术语 + "**DOG!**" 旁白标记 = seedance 看到"朗读"概念 → 激活人声生成路径
- ❌ **Pic10 Hamster v3 修复铁律倒退**（之前已沉淀 `seedance-prompt-翻车修复方法论-2026-06-10.md`）：
  - 完全删除元术语"TTS/音轨占位/朗读时长"等
  - 但 Dog prompt 中**保留了"旁白朗读" 4 字** = 埋雷
- ❌ 末尾约束段**没把"无朗读"放最前**（v22 官方原话顺序 = 无字幕/无水印/无 Logo/无人声/无歌唱/无配音/无朗读）—— 我**没**严格按这个顺序

**之前 skill 仓的约束（已存在但被违反）**：
- `references/分镜设计规范-v15director.md` §1[4] 末尾约束段官方原话（2026-06-10 沉淀）：
  ```
  保持无字幕、无水印、无 Logo，无人声、无歌唱、无配音、无朗读。
  ```
- `references/seedance-prompt-翻车修复方法论-2026-06-10.md`（Pic10 v3 修复实战）：
  - Step 1 修复方向：**完全删除元术语**"TTS / 音轨占位 / 朗读时长"等
  - Step 2 验证：跑 clip2 v3，**反复听 3 遍**确认无说话声
  - **md5 验证**：Pic10 v3 md5 `1d5a6466a4d51875c01f0c568877acc5` ✓
  - **用户原话**："这次修复了音频问题" ✅（修复通过信号）
- doc2 官方原话（已被 skill 引用 · line 534）："保持无字幕、无水印、无 Logo，无人声、无歌唱、无配音、无朗读"
- **官方文档 0 命中"TTS"概念**（之前已确认）—— "TTS" = 元术语 = 触发 seedance 激活人声路径

**修复方向（v1.2.0+pic24 升级建议）**：
1. **prompt 严禁"朗读/旁白朗读"等元术语**——直接写"声音"或"音效"或干脆不写
2. **末尾约束段顺序必须严格按官方原话**——无字幕/无水印/无Logo/无人声/无歌唱/无配音/无朗读（**不**打乱顺序）
3. **升 `prompt.禁元术语` 铁律**（4 问审查 = 元教训级）

**Dog prompt 实际情况（已确认违反）**：
- clip1: `旁白朗读："DOG!"` ← **元术语"朗读"埋雷**
- clip2: `旁白朗读："A cute dog."` ← 同上
- clip3: `旁白朗读："The dog runs fast."` ← 同上
- clip7: 同上 + 5 词 OG 家族

= **4/4 段全埋雷** = 不只 clip1，**理论 clip2/3/7 也可能有人声**

---

### C2：大部分视频生成了 BGM（铁律 #86 段 4 写法倒退 · 2026-06-10 18:35 反馈）

**用户反馈**："**大部分的视频都生成了BGM，这个之前也解决了的。**"

**根因**：
- ❌ **末尾约束段**虽然写了"无人声、无歌唱、无配音、无朗读"，**但漏了"无 BGM / 无背景音乐"** —— 段 4 兜底句不完整
- ❌ Cat 范本（**v1.0.6+pic16 沉淀**）的兜底句原话是"**无任何背景音乐、无旁白人声、无哼唱**"（3 项），**不是**v22 我改写的"无人声、无歌唱、无配音、无朗读"（4 项）= **Cat 范式回滚错了**
- ❌ 段 2 镜头里写"一声轻柔的微风声" / "一声轻快的脚步轻踏声"（音效嵌入视觉句）= seedance **把这些音效**解读为**背景音乐**或**拟声启用** → 激活 BGM 轨

**之前 skill 仓的约束（已存在但被违反）**：
- `references/sound-strategy-branches.md`（v1.0.6+pic16 Pic7 Horse 实战沉淀）：
  - **Cat 范本原话**（line 41-48）："**Cat 跑通的真实范本**（`assets/example-prompts/cat-clips-1-6-v15.1.txt`）里段 4 就是 **A 档兜底句"无任何背景音乐、无旁白人声、无哼唱"**（6/6 Clip 都是这样写）"
  - **2 维度 × 3 档** 声音策略分支表（line 50-54）：
    - A 档（默认）：`--generate-audio true` + 段 4 = "**无任何背景音乐、无旁白人声、无哼唱**" + 段 2 拟声嵌入
    - B 档（家族词组集合 ≥3 词）：`--generate-audio true`（**不**改 false）+ **同 A 档**段 4
    - C 档（长句 ≥5 词 OR 中文 ≥8 字）：`--generate-audio true` + **同 A 档**段 4
  - **Pic7 R7 翻车 1**（line 44）：家族词组 5 词走 `--generate-audio false` + 段 4 写"无任何背景音乐..." → 跑通但**全静音** = 用户反馈"音效要配合画面动态匹配"
  - **Pic7 R7 翻车 3**（line 46）：**真根因是 fill 模板的 `+朗读 'X 词'` 写法** = 用户怒吼"照搬 Cat 那一版"
  - **v1.0.6 Cat 范式回滚**（line 48）：**不动 v1.0.3 兜底句** + **不加 v1.0.4 具体音效清单** + **不加 v1.0.5 脱敏** = **Cat 范本原貌** = 跑通
- `SKILL.md` 铁律 #86（声音策略分支 · Pic6 Cow 实战沉淀）：
  - "**不生成发音+TTS 音轨对齐**"（3 旁白类型 × 声音策略分支表）
  - 引用：`references/sound-strategy-branches.md`
- `SKILL.md` 铁律 `prompt.末尾约束4词`：
  - "**末尾约束段 4 词官方原话**（保持无字幕、无水印、无 Logo，无人声、无歌唱、无配音、无朗读）"

**修复方向（v1.2.0+pic24 升级建议 · 已在 v15director §1[4] 应用）**：
1. **段 4 兜底句改回 Cat 范本原话**——"**无任何背景音乐、无旁白人声、无哼唱**"（3 项 · Cat 6/6 Clip 跑通）= **撤销 v22 我改写的 4 项版本**
2. **段 2 拟声嵌入要 `<>` 包裹**（官方 doc2 特殊字符表 · line 440-451）= 显式音效，不是 BGM
3. **`--generate-audio true`** 必加（**不**改 false · Pic7 翻车 1 教训）
4. **升 `prompt.段4兜底句-Cat原话` 铁律**（4 问审查 = 元教训级）

**Dog prompt 实际情况（已确认违反）**：
- clip1 末尾约束段："无人声、无歌唱、无配音、无朗读" ← **漏"无BGM"**
- clip2-3-7 同上
- 段 2 拟声没 `<>` 包裹（如 clip7 "一声轻快的脚步轻踏声"）= 触发生成 BGM 轨

= **4/4 段全违反段 4 兜底句 Cat 范本** = 必然出 BGM

---

### C3：运镜设计 6 分（v22 Step A 决定规则倒退 · 2026-06-10 18:35 反馈）

**用户反馈**："**视频的运镜设计，如果10分的话，我只能给6分，刚好及格**"

**根因**：
- ❌ **v22 Step A 看图决定景别 + 运镜**（5 种画面状态对应 5 种运镜）**没真用**—— 主 agent 跑分镜时**凭印象套"中景拉远" / "正面平视" / "固定中景"** = 6/6 段统一模板化 = 6 分（**及格但平庸**）
- ❌ **4 段 prompt 的运镜全是一个套路**：
  - clip1: "**固定中景**" + "尾巴翘起一动不动" = 5s = 几乎静态
  - clip2: "**中景拉远**" + 草地欢快小跑 = 一拉到底
  - clip3: "**正面平视**" + 飞奔 = 平视跟拍
  - clip7 镜 1-5: 都是"**正面近景**"或"**正面中景**" = 5 个相同景别
- ❌ **没按 5 种画面状态决定运镜**（v22 决定规则）：
  - 仓鼠蹲伏 = 固定中景（Pic10 实战）
  - 仓鼠从门洞探出 = 侧面机位看门洞（Pic10 实战）
  - 跑轮 = 正面平视（Pic10 实战）
  - 草地 = 拉远（Pic10 实战）
  - 静态 = 不动（Pic10 实战）

**之前 skill 仓的约束（已存在但被违反）**：
- `references/分镜设计规范-v15director.md` §7.6 Step A 看图决定景别+运镜（v1.2.0+pic22 新增）：
  - "Step A 看图决定景别 + 运镜（5 种画面状态对应 5 种运镜）"
  - "禁微风声作为默认兜底"
- `references/分镜设计规范-v15director.md` §1 核心原则第 5 条：
  - "**不是**'默认中景拉远定格 + 一声轻柔的微风声'（**Pic10 Hamster 5-8 段 6/6 段 100% 套用 = 翻车** · v1.2.0+pic22 新增红线）"
- `references/分镜设计规范-v15director.md` §1.3 反模式区（v1.2.0+pic22 新增）：
  - "❌ **默认'中景拉远定格 + 一声轻柔的微风声'模板**（**Pic10 Hamster 5-8 段踩过** · 6/6 段 = 100% 套用）→ 用户 2026-06-10 反馈'分镜设计过于模板化'· 实际应该是**按画面真实状态决定运镜 + 音效**"
- **Pic10 Hamster 实战沉淀**（v1.2.0+pic22 修复源）：
  - 仓鼠蹲伏 = 固定中景
  - 仓鼠从门洞探出 = 侧面机位看门洞
  - 跑轮 = 正面平视
  - 草地 = 拉远
  - 静态 = 不动

**修复方向（v1.2.0+pic24 升级建议 · 已在 v15director TL;DR 应用）**：
1. **主 agent 跑分镜时必走 v22 Step A**——5 种画面状态对应 5 种运镜（**不**是凭印象套）
2. **景别差异化**——clip1-8 不能都用"中景"或"正面"，要根据画面实际状态选
3. **升 `prompt.StepA强制化` 铁律**（4 问审查 = 元教训级）

**Dog prompt 实际情况（已确认违反）**：
- 4 段运镜单一：
  - clip1: 固定中景（5s 几乎静态）
  - clip2: 中景拉远
  - clip3: 正面平视
  - clip7: 5 镜都用"正面近景/中景"
- **没差异化** = 6 分（**及格但平庸**）

---

## 🟡 B 类（规划型 · v22 新规范漏洞 · 2026-06-10 17:55 已记录）

### B1：长词族 prompt 设计 = 物理装不下
- 5 词 OG 押韵家族 = 13.07s 总需求 = 6s 装不下
- v22 4 步原则没考虑长词族场景

### B2：v22 4 步原则的"动作步骤"概念对"词族"不适用
- 词族动作（5 词 = 5 节奏点 = 5 镜）≠ 物理动作
- v15director §7 应该新增"词族动作"类

### B3：v22 "按需要选合适"原则没显式沉淀
- 允许单段 1 镜/2-3 镜/5 镜/7-14s/多段拆分 = 都没明文
- v15director §7.7 应该新增"旁白长度决定时长档位"

### B4：当前"1 个 Clip 装 1 张图"默认 = 词族场景不够灵活
- 8 张图 = p7 词族 = 需要拆 2 段 = 8 张图变 9 段
- Step 1 启动前 7 必问的"切分方式"应该加例外

### B5：v22 新规范"端到端"还没真测过
- v22 修复了模板化（Step A/B/C 决定规则），但修复在 Hamster 还没真测过
- Dog 跑完后 = v22 真测 = 有 v1 (Hamster) vs v22 (Dog) 对比数据

---

## 🧠 元教训总结（v1.2.0+pic24 关键洞察）

### 洞察 1：**"skill 仓有约束 ≠ 主 agent 跑任务时真用"**

**8/11 问题 = 之前 skill 仓已有约束 · 主 agent 实际跑任务时又违反** = **强制化失败**。

**反模式**：
- ❌ 把 skill 写入文档就以为"已解决"
- ❌ 主 agent 跑新任务时**不主动查** skill 仓对应章节
- ❌ 主 agent 跑新任务时**凭印象 + 套历史范本** = 不查 skill 仓

**正模式**：
- ✅ 跑新任务前必走 `skill_view(name='picturebook-video')` + `skill_view(file_path='references/分镜设计规范-v15director.md')` + `skill_view(file_path='references/sound-strategy-branches.md')` = **强制查 skill 仓** = v1.2.0+pic24 新增必走步骤
- ✅ 写 prompt 前必走 §7.6 Step A/B/C 决策表 = **强制化**
- ✅ 写完 prompt 后必对照 §1[4] 末尾约束段原话 = **强制对账**

### 洞察 2：**"v22 我自己改写的 4 项版"是元凶**

**v22 末尾约束段**（我改写 4 项版）："无人声、无歌唱、无配音、无朗读"
**Cat 范本原话**（3 项版 · 6/6 跑通）："**无任何背景音乐、无旁白人声、无哼唱**"

**v22 4 项版 = 漏"无任何背景音乐" = Dog 4/4 段全出 BGM = 元凶**

**反模式**：
- ❌ 看到"v22 修复"就盲信
- ❌ 擅自"扩展"原话（v22 4 项版 = 我加的"歌唱/配音/朗读" 3 项 = 漏"无BGM"）
- ❌ 不重读 Cat 范本

**正模式**：
- ✅ 写新版本前**先翻 Cat 范本原话**（`assets/example-prompts/cat-clips-1-6-v15.1.txt`）
- ✅ **不要擅自改原话** = 改了就跑通不了
- ✅ 末尾约束段 = **金标准是 Cat 范本**（6/6 Clip 跑通 = 实证）

### 洞察 3：**"v1.2.0+pic22 修复只写在文档里 = 没真用"**

**v15director §7.6 Step A/B/C 决定规则**（v22 沉淀）：
- Step A 看图决定景别 + 运镜（5 种画面状态对应 5 种运镜）
- Step B 看图决定音效（5 种场景对应 5 种音效）
- Step C 镜头命名用"动作+状态"

**实际跑 Dog 时主 agent 凭印象套"中景拉远" / "正面平视" / "固定中景"** = **6 分**。

**反模式**：
- ❌ 写完 step A/B/C 在文档里 = 以为已落地
- ❌ 跑新任务时**凭印象** + 套历史范本 = 跳过 step A/B/C

**正模式**：
- ✅ **必走 step A/B/C 决策表** = 强制化（v1.2.0+pic24 升级）
- ✅ 写完 prompt 后**自检 3 问**：
  1. 这段的景别是不是按参考图实际状态定的？（不是凭印象）
  2. 这段的音效是不是按参考图实际场景定的？（不是默认微风）
  3. 这段的镜头名是不是用"动作+状态"命名？（不是"建立场景"标签）

---

## 🚦 状态

- **Dog 视频未全部开工** = clip1-3+7 已跑、clip4-6+8 待跑
- **本文件 = v1.2.0+pic24 升级依据 + 达尔文后续优化输入**
- **下一步建议**：
  1. **立即沉淀到 skill 仓**：升 6 条铁律（A1+A2+A3+C1+C2+C3）· 已部分应用
  2. **修 Dog 4 段 prompt**（v1.2.0+pic24 修复版）= 用 Cat 原话 + 删元术语 + 必走 Step A/B/C
  3. **重跑 Dog 8 段**（v1.2.0+pic24 单 Clip 端到端验证 → 通过后再批量）

---

**记录时间**：2026-06-10 18:35
**记录人**：主 agent（v1.2.0+pic22 净化后 + v1.2.0+pic23 流程漏洞修复后 + v1.2.0+pic24 视频质量修复中）
**数据来源**：
- B 类 = Dog 绘本规划阶段（2026-06-10 17:55）
- A 类 = Dog 绘本交付阶段（用户 2026-06-10 18:15/18:20/18:25 三次拍板）
- C 类 = Dog 绘本视频质量（用户 2026-06-10 18:35 三次拍板）
- 修复 = v15director §1[4] 已应用（v1.2.0+pic24 部分修复）
