# v9 · v7 + 首尾帧实验

**测试时间**：2026-06-03 早
**结果**：⚠️ 备选知识库（已弃用为绘本默认）

## 范式核心
v7 增量：探索 `--image` + `--last-frame` 首尾帧控制 vs v7 的多帧参考 + `@Image1/@Image2` 引用。

## 结论
- **首尾帧 vs 多帧参考是两种不同范式**——互斥
- v7/v10 的"多帧参考 + @ImageN 引用"是绘本默认方向
- 首尾帧保留为**备选知识库**（电影感运镜场景可能用）

## 关键教训
用户原话："我用的不是首尾帧，绝对不是首尾帧"——v10 跑通时差点踩坑。

## 状态
⚠️ 备选知识库——绘本场景不用，移到 📚 Seedance 2.0 其他能力备选知识库

## 检索词
`首尾帧` / `--last-frame` / `last-frame vs multi-frame` / `多帧 vs 首尾帧`
