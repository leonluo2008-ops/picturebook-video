# v11-β · no_hold_pose + camera_drift（AB test 胜出）

**测试时间**：2026-06-03 下午
**绘本**：Cactus 仙人掌
**结果**：⚠️ 备选知识库（v0/v1/v2/v1v2_combo AB test 中 v1v2_combo 胜出）

## 范式核心
v10 增量：探索两个 prompt 修饰词对镜头运动的影响：

| 变体 | 关键句 |
|---|---|
| **v0 baseline** | 无特殊修饰 |
| **v1 no_hold_pose** | prompt 中不写"hold steady"（给镜头运动空间）|
| **v2 camera_drift** | 加 `camera gently drifts during this shot` |
| **v1v2_combo** | v1+v2 组合 |

## 结论
- v1v2_combo 镜头运动最自然（不静止、轻微漂移）
- v0 太静（v6/v7 风格）
- v1 / v2 单独都不够

## 状态
⚠️ 备选知识库——v10 没采用，v14 也没采用（v14 用精准分镜+收势锁尾帧）

## 检索词
`AB test` / `camera_drift` / `no_hold_pose` / `镜头运动`
