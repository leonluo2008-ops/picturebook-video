# v13 · v12 + 主体定义（部分修复）

**测试时间**：2026-06-04 上午
**任务 ID**：cgt-20260604144202-zqprm
**结果**：⚠️ 部分（角色对位 OK 但只看到图 1 元素）

## 范式核心
v12 修复：在 prompt 开头加主体定义：
```
将图片1中的 [主体核心特征] 定义为主角@图片1
```

## 解决了什么
- 角色对位 OK（不覆盖参考图主体）
- 风格锚定（2D 纸艺不漂移）

## 暴露的问题
**只看到图 1 元素**——镜头 2 切换到图 2 时，画面里只看到图 1 的元素（场景/道具）。

## 关键教训
主体定义解决了"主角是谁"，但**没解决"镜头 2 用图 2 的什么元素"**——必须让每个镜头显式绑定到不同图片元素。

## 状态
⚠️ **部分范式**——v14 升级

## 修复方向
→ v14 每个镜头显式绑定到不同图片元素
