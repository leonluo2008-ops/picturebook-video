# 声音策略分支（Sound Strategy Branches · v6 第 4 段声音维度微调）

> **铁律 #86 候选 · v1.0.3+pic13 新增**（Pic6 Cow 牛 clip7 实战沉淀 · 2026-06-08）
> 声音维度的"分支判断"·**不破坏 4 维控制底层核心**。
> **触发条件**：旁白 = 家族词组集合（≥3 词 + 同字母家族重复）OR 长句（英文 ≥ 5 词 / 中文 ≥ 8 字）

---

## 0. 为什么需要这个分支

**用户 2026-06-08 实战反馈原话**：
> "像这种家族词组的集合，在视频里不需要生成发音，用 TTS 音频来做对齐就可以了。...但是我并不想破坏底层核心啊，这只适合这种家庭词组，或者说句子很长的时候的一种方式。"

**根因**：
- seedance `--generate-audio true` 时会**自动生成拟声/朗读**作为参考音频层的补充
- 家族词组（cow, bow, now, how, wow）= 5 个独立音节 → seedance 容易**重复发/抢节奏/发音错位**（Pic6 clip7 v1 实测）
- 长句（>5 词）= 音节密度高 → seedance 容易**吞字/抢拍/语气错**
- 用户提供 TTS 音频时 = 已有准确音轨 → seedance 自动生成 = **冗余且翻车风险高**

**本分支目的**：
- 4 维控制底层核心**不动**（时间/风格/角色/声音 4 维照走）
- 仅在**声音维度**加一条"分支判断"——根据旁白类型选 seedance 音频生成策略
- 避免**重复发/抢节奏/发音错位**翻车
- TTS 音轨对齐 = 唯一声音源

---

## 1. 底层 4 维控制（不动）

| 维度 | 内容 | 是否修改 |
|---|---|---|
| **时间** | 节奏公式 v5/v6（朗读完最低 3s + 末帧静默 ≥ 2s）| **不动** |
| **风格** | 段 3 风格锁（v15 4 段 / v6 5 段结构）| **不动** |
| **角色** | 段 1 主体定义（视觉特征字典 + @ImageN）| **不动** |
| **声音** | 段 4 BGM 段 + 拟声 + 朗读策略 | **加分支** ← 本文件 |

---

## 2. **2 维度 × 3 档** 声音策略分支表（v1.0.6+pic16 修复 · 2026-06-09 Pic7 Horse R7 实战沉淀 · Cat 范式回滚）

> **v1.0.6 修复**（撤销 v1.0.4 矫枉过正）：v1.0.4 改成"3 维度 × 5 档"（朗读/拟声+画面音效/环境音独立开关）太复杂。**Cat 跑通的真实范本**（`assets/example-prompts/cat-clips-1-6-v15.1.txt`）里段 4 就是 **A 档兜底句"无任何背景音乐、无旁白人声、无哼唱"**（6/6 Clip 都是这样写）—— **Cat 段 2 镜头里**写"猫跃入 咚 一声"（音效嵌入视觉句）—— **音效靠段 2 拟声字段，不靠段 4 BGM 段**。
>
> **Pic7 实战教训链**（三轮翻车沉淀 · 2026-06-09）：
> 1. **Pic7 R7 翻车 1**（v1.0.3 默认）：家族词组 5 词走 `--generate-audio false` + 段 4 写"无任何背景音乐..." → 视频跑通但**全静音**（卡片高亮+5 词切换+标题呼吸全无声音）= 用户反馈"音效要配合画面动态匹配"
> 2. **Pic7 R7 翻车 2**（v1.0.4 矫枉过正）：B 档段 4 改写"画面元素动作音效保留（卡片高亮配叮，叉子配叮咚...）"——**主 agent 在段 4 加了具体音效清单** = 翻车（v1.0.5 反馈"v1.0.4 B 档的具体音效清单是错模式"）
> 3. **Pic7 R7 翻车 3**（v1.0.5 矫枉过正）：fill 模板加脱敏映射 + `narration_marker` 改"目标词"——**真根因是 fill 模板的 `+朗读 'X 词'` 写法** + "目标词"标识**都破坏了 Cat 范本** = 用户怒吼"照搬 Cat 那一版"
>
> **v1.0.6 Cat 范式回滚**：**不动 v1.0.3 兜底句** + **不加 v1.0.4 具体音效清单** + **不加 v1.0.5 脱敏** = **Cat 范本原貌** = 跑通。

| 旁白类型 | `--generate-audio` | 段 4 BGM 段写法 | 段 2 镜头描述（音效嵌入视觉句）| 段 5 文字持续可见 |
|---|---|---|---|---|
| **A. 普通短句/单词**（默认）| `true` | "无任何背景音乐、无旁白人声、无哼唱" | "小猫 咚 一声跃入" / " 叮 一响清亮"（拟声嵌入视觉句，用 `,` 串接）| 全程可见 + 微动画 |
| **B. 家族词组集合**（≥3 词 + 同字母家族重复）| `true`（**不**改 false）| **同 A 档**（"无任何背景音乐、无旁白人声、无哼唱"）| "镜头定格在 cat 卡片，叮 一响，猫转头看向卡片"（Cat 范本里就是这么写的）| 全程可见 + 微动画 |
| **C. 长句**（words_en ≥ 5 OR words_zh ≥ 8）| `true` | **同 A 档** | "镜头从近景拉远到中景， 沙 纸张翻动声"（视觉动作 + 拟声嵌入）| 全程可见 + 微动画 |

**判定优先级**（从高到低）：
1. 家族词组集合（≥3 词 + 同字母家族重复）→ B 档（**只判定 · 不改段 4 写法**）
2. 长句（words_en ≥ 5 OR words_zh ≥ 8）→ C 档（**只判定 · 不改段 4 写法**）
3. 普通短句/单词 → A 档

**Pic7 实战坑 1**（v1.0.3 旧）：家族词组走"拟声关闭"= 卡片高亮+5 词切换+标题呼吸全无声音。**修复方向（v1.0.6 Cat 范式回滚）**：**段 2 镜头里**写"卡片浮现 叮 一响清亮"+ "标题 'OR Family' 整体呼吸1次"——**音效靠段 2 拟声字段**（嵌入视觉句）= 跟画面动态匹配。**段 4 不变**（仍然 A 档兜底句）。

**Pic7 实战坑 2**（v1.0.4 矫枉过正）：B 档段 4 写"画面元素动作音效保留（卡片高亮配叮，叉子配叮咚，号角配呜，玉米配轻摆沙沙，pork 配轻响），不发完整朗读，TTS 后期对齐"——**反模式**。**Cat 范本里段 4 就是 A 档兜底句**——音效**全在段 2 镜头描述里**（"猫跃入 咚 一声" / "卡片 叮 一响"）。

**Pic7 实战坑 3**（v1.0.5 矫枉过正）：fill 模板加脱敏映射 + `narration_marker` 改"目标词"——**反模式**。**Cat 范本里 narration_marker = "（朗读 1s + 静默 2s 停留，镜头停在画面上让听众消化单词）"**——**C 写的"cat 卡片"原样保留**——**音效嵌入视觉句**（"卡片 叮 一响"）——seedance 看到"朗读 1s"知道要留时间但不强制生成具体朗读。

**Pic7 实战坑 4**（v1.0.5+pic14 段 2 拼"+朗读 X 词"翻车）：主 agent 在段 2 加"朗读 '家族词组'"/"朗读 '第2词'"等 5 条指令 = seedance 把每个当独立朗读指令执行 = 5 次独立朗读 = 抢节奏+错乱。**修复**：段 2 narration_marker 只写"（朗读 Ns + 静默 Ns 停留）"——不写具体目标词——TTS 后期对齐。

---

## 3. seedance 命令差异（v1.0.6+pic16 Cat 范式回滚）

| 旁白类型 | `--generate-audio` | 段 4 prompt 写法 | 段 2 拟声字段 |
|---|---|---|---|
| A. 普通短句/单词 | `true` | "无任何背景音乐、无旁白人声、无哼唱" | 拟声嵌入视觉句（"小猫 咚 一声跃入"）|
| **B. 家族词组集合** | `true`（**不**改 false）| **同 A 档**（"无任何背景音乐、无旁白人声、无哼唱"）| 拟声嵌入视觉句（"卡片 叮 一响，猫转头看向卡片"）|
| **C. 长句** | `true` | **同 A 档** | 拟声嵌入视觉句（" 沙 纸张翻动声"）|

**Pic6 clip7 实战（v1.0.3 旧）**：家族词组（cow, bow, now, how, wow 5 词）= `--generate-audio false` + 段 4 prompt 写"无任何背景音乐" → 视频跑通无重复发音 ✅（但**没验证音效在不在**——Pic7 才暴露问题）

**Pic7 clip4 实战（v1.0.6 修复 · Cat 范式回滚）**：家族词组 OR 5 词（horse/fork/horn/corn/pork）= `--generate-audio true` + 段 4 prompt 写 A 档兜底句"无任何背景音乐、无旁白人声、无哼唱" + 段 2 拟声嵌入视觉句"卡片 叮 一响清亮" / "图标浮现 叮咚 一响" / "号角旋转 呜 一响" / "玉米棒摆动 轻摆沙沙 一响" → 视频跑通 0 重复发音 + 卡片高亮+5 词切换+标题呼吸**全有音效匹配**（task `cgt-20260609204142-7wbhp` · seed 23560 · 4.80 MB · 255s 完成）。

**判断口诀**："**段 4 = A 档兜底句（不展开）· 段 2 拟声 = 视觉句里嵌入 = 跟画面动态匹配**"。

---

## 4. 触发条件判定（v6 fill 模板 B 旁白量化必填）

`narration-quantization.json` 每条记录加 `sound_strategy` 字段：

```json
{
  "id": 7,
  "en": "cow, bow, now, how, wow",
  "zh": "奶牛 cow 的 OW 家族...",
  "words_en": 5,
  "words_zh": 5,
  "sound_strategy": "family_word_set",  // ← 新字段
  "sound_strategy_rationale": "5 词同字母家族重复（OW）· 走分支·不生成发音+TTS 对齐"
}
```

**枚举值**（v1.0.6 简化 · 3 档 · 对应上表 A/B/C）：
- `A_standard`：普通短句/单词（默认 · 朗读+拟声+环境音 全开 · A 档兜底句）
- `B_family_set`：家族词组集合（≥3 词 + 同字母家族重复 · 走 B 档 · **不**改 `--generate-audio` · **不**改段 4 写法 · 段 2 拟声嵌入视觉句）
- `C_long_sentence`：长句（words_en ≥ 5 OR words_zh ≥ 8 · 走 C 档 · 同 A 档写法）

**判定逻辑**（B 子 agent / 主 agent 干 B 时必跑 · v1.0.6 简化）：
```python
def detect_sound_strategy(words_en, words_zh, en_text):
    """
    v1.0.6+pic16 简化 · 3 档声音策略判定（Cat 范式回滚）
    
    Parameters
    ----------
    words_en : int
    words_zh : int
    en_text : str
        英文文本（用于家族词组结尾检测）
    
    Returns
    -------
    strategy : str
        A_standard / B_family_set / C_long_sentence
    rationale : str
        判定理由
    """
    # 1. 家族词组集合判定（≥3 词共用相同 2 字母结尾）
    if words_en >= 3 and ',' in en_text:
        words = [w.rstrip('!.,?').lower() for w in en_text.split(',')]
        suffixes = [w[-2:] for w in words if len(w) >= 2]
        if len(suffixes) >= 3 and len(set(suffixes)) == 1:
            return 'B_family_set', f'家族词组集合（{len(words)} 词 · 共同 2 字母结尾 {suffixes[0]}）· B 档：段 4 仍 A 档兜底句 + 段 2 拟声嵌入视觉句'
    
    # 2. 长句判定 → C 档
    if words_en >= 5 or words_zh >= 8:
        return 'C_long_sentence', f'长句（words_en={words_en} OR words_zh={words_zh}）· C 档：同 A 档写法'
    
    # 3. 普通短句/单词 → A 档
    return 'A_standard', f'普通短句/单词（words_en={words_en} words_zh={words_zh}）· A 档：默认全开'
```

**v1.0.6 简化原因**：v1.0.4 5 档（A/B/C/D/E）+ 3 维度独立控制太复杂，Cat 跑通验证**段 4 不变**（A 档兜底句）——音效全在段 2 拟声字段里（嵌入视觉句）。**D/E 档实战没用到**——下本（Pic8）真需要时再加。

---

## 5. 与 v6 fill 模板的衔接（暂未联动）

**v1.0.3+pic13 决策**（Pic6 实战）：

✅ **写 reference 文档**（本文件）— 沉淀判断逻辑
❌ **不**直接改 v6 fill 模板 — 等 Pic7/Pic8 实战验证"家族词组判定"准不准
⏳ **下本（Pic7/Pic8）实战后再考虑升格**为 B 字段联动 + fill 模板自动读字段

**理由**：
- OW 家族（5 词 + -ow 重复）= 典型家族
- AY/EE/AR 家族 = 实战未验证
- 判定规则可能反复调（OW 用"后 2 字母重复"，AY 也用，AR 结尾不同可能要改"含相同字母家族"广义规则）

---

## 6. Pic6 clip7 实战验证

| 维度 | 修复前（v1 默认）| 修复后（本分支）| 结果 |
|---|---|---|---|
| `--generate-audio` | true | false | ✅ 关闭拟声自动生成 |
| TTS 音轨 | 无 | 预留 | ✅ TTS 插入位明确 |
| seedance 5 词发音 | 易重复/抢节奏 | 不生成 | ✅ 干净 |
| 末帧微动（呼吸+OW 高亮+卡片上浮）| 受音频生成干扰 | 不受干扰 | ✅ 5 个微动元素 100% 保留 |
| 时长 10.10s | 设计 10s + ceiling 0.10s | 同 | ✅ 铁律 #72 整数 |

**用户反馈**：「clip7，本轮的 clip7，实际上非常好，就是符合我提供的时长，并且声音也符合需求。」

---

## 7. 反模式（不要做）

| 反模式 | 后果 |
|---|---|
| 把"不发音+TTS 对齐"作为默认策略 | 普通短句被剥夺拟声 = 翻车（"moo!" 单音节拟声是领读绘本灵魂）|
| 把"不发音+TTS 对齐"硬编码到 v6 段 4 模板 | 破坏模板通用性 · Pic5 Bird 等非家族词组也走静音 |
| 不分旁白类型一刀切 `--generate-audio false` | 全部静音 = 翻车（"moo!" 拟声 = 知识向核心）|
| 改 4 维控制底层核心 | 破坏通用框架 · 用户原话「不想破坏底层核心」|
| **段 2 写"朗读 X 词"指令**（v1.0.5+pic14 实战新增 · 2026-06-09 Pic7 Horse R7 翻车）| seedance 把每个"朗读 X 词"当独立朗读指令 → 家族词组 5 词 = 5 次独立朗读 → 抢节奏 + 重复发 + 错乱（用户反馈"完全错乱的语音"）。**段 2 = 镜头描述 + 拟声**（音效嵌入视觉句，用 `,` 串接）**不**是朗读台词表。**TTS 朗读 = 后期对齐**（不在 prompt 里指挥）。|
| **段 4 写具体音效清单**（v1.0.5+pic14 实战新增）| 段 4 BGM 段 = **声音策略的兜底句**（A 档/B 档/C 档/D 档/E 档 标准句），**不**是拟声清单。**具体音效描述**（"卡片高亮配叮/图标浮现配叮咚..."）属**段 2 镜头描述末尾的拟声字段**（紧贴动作，用 `,` 串接），不属段 4。段 4 写"卡片高亮配叮..."会让 seedance 把它当音效清单执行 → 混乱生成。|
| **段 2 文字保留复制 2 遍**（v1.0.5+pic14 实战新增）| v15/v6 段 2 文字保留 = **1 段独立约束**。v6 场景下，段 2 文字保留（位置锁）+ 段 5 文字持续可见段（全程可见 + 微动画）= **互补不重复**。把段 5 的"全程可见"复制到段 2 强化 = 重复 + 模型困惑 + 段 2 句号断句切碎视觉。|

---

## 8. 关联铁律 / 文件

- **picturebook-video/SKILL.md**：铁律清单 + 4 维控制原理
- **references/v6-5段骨架-模板.md**：v6 5 段结构定义
- **references/v15-4段骨架-模板.md**：v15 4 段结构定义
- **references/旁白朗读时长计算.md**：B 旁白量化公式
- **Pic6 Cow 牛 实战报告**（待写）：clip7 5 词设计 + 声音策略分支验证

---

## 9. 检索词

`声音策略分支` / `sound_strategy` / `家族词组集合` / `family_word_set` / `TTS 音轨对齐` / `不发音+TTS 对齐` / `4 维控制底层核心` / `铁律 #86` / `v1.0.3+pic13` / `Pic6 Cow clip7`
