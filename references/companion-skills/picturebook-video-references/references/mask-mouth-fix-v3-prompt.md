# 口罩漏嘴 v3 修复完整配方（2026-08-31 牙医 clip3 实测一次过）

> 配套 §24。场景：人物戴口罩 + 旁白含「张大嘴巴」类口部动作词 → 模型把动作渲染到人物口罩上。

## 1. 根因与判据

- v1/v2 症状：白色口罩上出现嘴/牙齿/口腔开口，8 帧复查反复命中。
- 根因：旁白动作词语义渗漏到人物（戴人口罩），提示词里只有动作描述没有“动作属于谁”的强绑定。

## 2. v3 prompt 策略骨架（在 v2 已过项之上追加）

1. 段 1 开头即写：`最高优先级铁律：小牙医口罩是完整纯色布料，绝对没有嘴、没有牙齿、没有舌头、没有口腔、没有任何开口，从头到尾一帧都不变；人物全身绝无任何口型动作，全画面唯一可以张开的只有桌上牙齿模型的上排牙齿。`
2. 人物描写降低面部细节倾向：`侧身约45度中景平视，身体朝向画面左侧的桌子，面部呈四分之三侧脸、只占画面很小部分` + `白色医用口罩为纯色无任何图案、完整贴合遮住口鼻且面积尽量小`。
3. 段 2 末尾、段 3 结尾再各完整重复一次口罩负面铁律（共 ×3）。
4. 段 3 语义绑定句式：`"张大嘴巴"这句旁白说的是牙齿模型的动作——牙齿模型在念到"张大嘴巴"时上排牙齿向上抬起、与下排分离明显张大，小牙医用右手指向模型做讲解示范`。
5. 已过项原样保留：双臂双手五指、桌面道具按参考图、牌面 `dentist`（小写，d红/e橙黄/n蓝/t橙/i粉红点/s浅蓝/t绿）+ 棕色`牙医`、旁白逐字「牙医说张大嘴巴，dentist」、/ˈdɛntɪst/ 音标 DEN-tist、朗读明显放慢、无字幕无Logo无水印无BGM、「叮」音效、口罩全程完整覆盖口鼻。

## 3. 提交脚本断言（submit_clip3_v3.py 惯用法）

```python
assert prompt.count('绝对没有嘴、没有牙齿、没有舌头、没有口腔、没有任何开口') == 3, 'mask negative must appear exactly 3x'
assert '侧身约45度' in prompt, 'side-45 constraint missing'
# body 字段防漂移断言：无 resolution/service_tier/watermark；content[0]=text, content[1] role='reference_image'
# ref 经 upload_to_uguu 上传后以 resolved_urls 映射进 build_body；成功 task_id 追加写 tasks.json
```

## 4. 8 帧放大复查配方（唯一验收 = 口罩全帧干净）

```python
# 720x1280 帧，人物脸-胸口区裁切框（已实测含口罩）：
crop = (260, 350, 680, 800)   # 3x 后 cell=1260x1350
# 0.5s 步进抽 8 帧 f_001..f_008 → 单帧 f_004 先确认框内含口罩（本次首版框截高只有头发+眼睛，作废）
# → 2列x4行拼表 + 时间戳标签 → vision_analyze 逐帧问嘴/牙/舌/口腔/开口/破洞 + 口罩有无消失移位
```

**验收回执口径**：8 帧全部干净 = 通过；任何一帧命中 = 按预案升版（v4：口罩约束再翻倍 + 人物站画面右侧远离镜头），每次回传结论，满 3 次止。本批 v3 一次过（8 帧 0.0–3.5s 全干净），终审拼图 mask_zoom_v3_sheet2.jpg。